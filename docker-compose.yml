version: '3.8'
services:
  cassandra:
    image: cassandra:latest
    container_name: cassandra-hidroponia
    ports:
      - "9042:9042"
    volumes:
      - ./data:/var/lib/cassandra
    environment:
      - CASSANDRA_CLUSTER_NAME=HydroCluster
